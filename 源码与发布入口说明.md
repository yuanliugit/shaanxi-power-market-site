# 陕电现货观察网站 — 源码与发布入口说明

> 生成日期：2026-07-28 ｜ 更新日期：2026-07-30
> 适用项目：陕西电力现货市场数据看板（Apple 风格单页应用）
> **公网访问地址：https://yuanliugit.github.io/shaanxi-power-market-site/**（GitHub Pages 长期托管）
> Git 仓库：https://github.com/yuanliugit/shaanxi-power-market-site（public，main 分支自动部署）

---

## 当前部署状态（2026-07-30 更新）

| 项目 | 状态 |
|---|---|
| 托管平台 | ✅ GitHub Pages（已生效） |
| 公网域名 | `https://yuanliugit.github.io/shaanxi-power-market-site/` |
| 仓库可见性 | public（原 private，已改公开以启用免费 Pages） |
| 自动部署 | push 到 main → GitHub Pages 自动重建（约 30-60 秒） |
| HTTPS | 强制启用 |
| 长期有效性 | 项目在则域名在；账号不违规、不主动删项目即可持续访问 |
| 备选方案 | 腾讯云 EdgeOne Pages（境内加速 + 自定义域名，见 `EdgeOne_Pages_部署指引.md`） |

---

## 一、网站源码目录

源码位于本地工作区，**当前未初始化为 Git 仓库**：

```
/Users/ly/WorkBuddy/2026-07-27-23-31-38/output/
├── index.html      # 约 41KB 页面外壳，运行时读取 data 下的 JSON
├── apple.css       # Apple 风格样式表（SF Pro 字体 / 系统蓝 / 圆角卡片 / 毛玻璃）
├── build_html.py   # 构建脚本（生成页面外壳并同步已校验数据）
├── data/
│   ├── snpx_spot_history.json
│   ├── wechat_wind_solar_prices.json
│   └── data_snapshot.json
├── downloads/
│   └── 陕西现货市场每日出清价格跟踪_2025至今_含风光分项.xlsx
└── 陕西现货市场每日出清价格跟踪_2025至今_含风光分项.xlsx  # 兼容旧下载位置
```

### 原始交付包（参考）

完整的 Next.js 16 + React 19 源码、数据 JSON、自动化脚本位于：

```
/Users/ly/WorkBuddy/2026-07-27-23-31-38/deliverables/陕西现货电价网站_Agent交付包/
├── website/              # Next.js 源代码（可独立构建）
├── data/                 # 结构化 JSON 数据 + Excel 底表
├── docs/                 # 数据字典、AI Agent 任务说明、日更工作流
├── automation_reference/ # 抓取、OCR、工作簿生成、日更脚本
└── manifest.json         # 交付包版本与数据范围
```

### Git 仓库状态

- `output/` 目录：**已初始化 Git 并推送**
- 远程仓库：`https://github.com/yuanliugit/shaanxi-power-market-site.git`（public）
- 当前部署：**GitHub Pages**，监听 `main` 分支自动部署
- 公网地址：`https://yuanliugit.github.io/shaanxi-power-market-site/`

---

## 二、CodeBuddy 发布入口

> ⚠️ 重要：以下三条产品线容易混淆，能力边界不同。

### 产品线对比

| 入口 | 形态 | 部署能力 | 适用场景 |
|---|---|---|---|
| **WorkBuddy 桌面端** | AI Agent 桌面应用（当前运行环境） | 仅 `workbuddy_cloudstudio_deploy` 工具 → CloudStudio 沙箱 shareLink | 一次性分享 / 快速预览 |
| **CodeBuddy IDE** | 独立 IDE 产品 | 一键部署到 CloudStudio / **EdgeOne Pages** / Lighthouse / TCB / Supabase | 可视化开发 + 部署 |
| **CodeBuddy CLI**（CodeBuddy Code） | 命令行 AI 工具 | 自然语言驱动，"一句话部署"，可嵌入 Shell 脚本 | 脚本化 / 远程 / CI 集成 |

---

### 1. Git 自动部署 → EdgeOne Pages

CodeBuddy 生态中**真正基于 Git 的自动部署方案**：

- **工作方式**：代码推送到 Git 仓库 → 自动触发部署
- **绑定域名后长期有效**（未绑域名含中国大陆仅 3 小时）
- **入口位置**：CodeBuddy IDE 内的「集成」按钮 → 选择 EdgeOne Pages
- **WorkBuddy 连接器**：列表中存在 `edgeone-pages EdgeOne Makers`（当前未连接，需先连接才能使用）

#### 各部署平台有效期对照

| 平台 | 业务能力 | 国际/国内 | 有效期（简要） |
|---|---|---|---|
| **Cloud Studio** | 部署 | 国际 & 国内 | 国内：链接 2 小时；国际：长期有效 |
| **EdgeOne Pages (EOP)** | 部署 | 国际 & 国内 | 不含大陆：长期；含大陆：3 小时，绑域名后长期 |
| **Lighthouse** | 部署 | 国内 | 依托云服务器，实例在则长期有效 |
| **TCB（腾讯云开发）** | 数据库 / 部署 | 国内 | 新环境 3 小时，绑域名后长期；旧环境长期 |
| **Supabase** | 数据库 | 国际 & 国内 | 项目有有效期，过期需在控制台激活 |

> ⚠️ 当前部署使用的 CloudStudio 沙箱链接**国内 2 小时有效**，不适合长期托管。如需长期稳定访问，建议迁移到 EdgeOne Pages 或 Lighthouse。

---

### 2. CLI 发布 → CodeBuddy CLI（CodeBuddy Code）

- **产品定位**：面向专业工程师的 AI 命令行工具，自然语言驱动开发全流程
- **核心能力**：
  - 无界面依赖，支持 SSH 远程开发、服务器等无 GUI 场景
  - 可嵌入 Shell / Python 脚本，实现开发流程自动化
  - 接入 MCP 协议，打通测试、构建、部署主流程
  - "一句话部署上线"
- **环境要求**：Node.js LTS
- **文档入口**：<https://www.codebuddy.ai/docs/zh/ide/Introduction>
- **注意**：这是独立产品，不在 WorkBuddy 桌面端内运行

---

### 3. API 发布

- **当前状态**：**无公开 REST API** 用于编程式发布站点
- 官方文档未提供独立的 HTTP 发布接口
- WorkBuddy 内的 `workbuddy_cloudstudio_deploy` 是内置工具调用，非对外开放的 API 端点

---

## 三、部署架构说明（当前实现）

```
┌─────────────────────────────────────────────────────────────┐
│  本地源码                                                    │
│  /Users/ly/WorkBuddy/2026-07-27-23-31-38/output/            │
│  ├── index.html  (自包含：CSS + JS + 数据)                  │
│  ├── apple.css                                                │
│  ├── build_html.py                                            │
│  └── *.xlsx                                                   │
└────────────────────────┬────────────────────────────────────┘
                         │ workbuddy_cloudstudio_deploy
                         │ (文件上传，非 Git)
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  CloudStudio 沙箱                                            │
│  sandboxId: c051b6840d434a74bac3dc05d7389f51                │
│  分享链接: https://c051b6840d434a74bac3dc05d7389f51.app.codebuddy.work  │
│  有效期: 国内 2 小时 / 国际长期                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、推荐升级路径

如需**长期稳定 + 可版本管理 + 自动部署**，建议按以下步骤升级：

### Step 1：初始化 Git 版本管理
```bash
cd /Users/ly/WorkBuddy/2026-07-27-23-31-38/output/
git init
git add .
git commit -m "init: 陕电现货观察 Apple 风格单页应用"
```

### Step 2：推送到远程仓库
- 可选：GitHub / GitCode / CNB / Gitee
- 推送后获得可追溯的版本历史

### Step 3：接入 EdgeOne Pages 做 Git 自动部署
1. 在 WorkBuddy 中连接 `edgeone-pages` 连接器（当前未连接）
2. 或在 CodeBuddy IDE 中通过「集成」按钮选择 EdgeOne Pages
3. 绑定 Git 仓库 → 后续 `git push` 自动触发部署
4. 绑定自定义域名 → 长期有效

### Step 4（可选）：数据更新流程
```bash
# 1. 在可维护网站项目中同步通过质量门禁的数据
npm run sync:data
# 2. 构建并运行测试
npm test
# 3. git 提交并推送，自动部署
git add app public scripts tests package.json
git commit -m "update: 数据快照更新"
git push
```

---

## 五、关键文件与命令速查

| 用途 | 命令 / 路径 |
|---|---|
| 本地预览 | `python3 -m http.server 8765`（在 output/ 目录下） |
| 重新构建 HTML | `python3 build_html.py` |
| 同步已校验数据 | `npm run sync:data`（在 `website/` 目录） |
| 当前公网链接 | <https://c051b6840d434a74bac3dc05d7389f51.app.codebuddy.work> |
| 管理已发布应用 | WorkBuddy「设置 - 数据管理 - 我发布的应用」 |
| CodeBuddy CLI 文档 | <https://www.codebuddy.ai/docs/zh/ide/Introduction> |
| EdgeOne Pages 连接器 | WorkBuddy 连接器列表中 `edgeone-pages`（需连接） |

---

## 六、数据范围与口径

- **市场整体**：2025-01-01 至 2026-07-13，共 559 个自然日
- **风电、光伏分项**：2025-04-06 至 2026-07-18，共 468 条有效记录
- **数据快照生成时间**：2026-07-20
- **价格单位**：元 / MWh
- **数据来源**：
  - 市场整体：陕西电力交易中心市场数据页面
  - 风光分项：公开发布的陕西现货市场日报（经图表识别、字段校验）

---

*本文档由 WorkBuddy 根据项目实际部署情况整理生成。*

---

## 七、2026-07-28 自动更新改造

- 总体价格和电量仅使用陕西电力交易中心结构化数据；公众号数据只补充风电、光伏分项，不再填补或覆盖总体字段。
- 页面改为运行时读取独立 JSON，数据不再内嵌到 1.2MB HTML；后续更新数据文件即可，不必修改页面代码。
- 新增 `data_snapshot.json`，动态展示总体与风光分项最新日期、质量门禁、源站缺口、双源差异和 OCR 复核状态。
- 新增 `scripts/sync-verified-data.mjs`：只有质量门禁、日期、重复值、Excel 完整性及公式检查通过后，才同步 JSON 和下载工作簿。
- 数据加载失败时页面明确提示，不使用公众号合计替代总体值，也不将空值自动填 0。
- 当前已校验数据范围：总体与风光分项均更新至 2026-07-26。
